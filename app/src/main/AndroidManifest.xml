<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!--
 minsdk is 9 10.24
 <uses-permission
        android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />
        used for packageManager.getInstalledPackages(0)??  or getPackageInfo??
        <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" /> no for Ando 10+
 <action android:name="android.intent.action.TTS_SERVICE" />
 <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" tools:ignore="QueryAllPackagesPermission" />
 adb shell bmgr restore trust.jesus.discover
 dumpsys backup
    -->
    <queries>
        <intent>
            <action android:name="android.intent.action.TTS_SERVICE" />
        </intent>
    </queries>
    <!--suppress AndroidElementNotAllowed -->
    <queries>
        <intent>
            <action android:name="android.speech.RecognitionService" />
        </intent>
    </queries>
    <application
        android:name=".little.Globus"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".actis.AyWelcome"
            android:exported="false"
            android:label="@string/title_activity_ay_welcome"
            android:theme="@style/AppTheme" />
        <!--
    25.8.25  android:theme="@style/AppTheme"  = fullscreen siehe main.kt
    android:theme="@style/Theme xx noActionbar = fullscreen?!
    https://www.geeksforgeeks.org/android/how-to-enable-full-screen-mode-in-android/
    org: android:theme="@style/Theme.Discover">
    Theme.Material3.DayNight.NoActionBar
    versmix:
    app: @style/Theme.AppCompat.DayNight
    activity @style/NoActionBar or  @style/Theme.AppCompat.NoActionBar
    aliwatch
    app: @style/AppTheme
    activity alle ohne..
        -->
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND" />

                <category android:name="android.intent.category.DEFAULT" />

                <data android:mimeType="*/*" />
            </intent-filter>
        </activity>
        <activity
            android:name=".actis.BibleAy"
            android:exported="false"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".preference.SettingsRand"
            android:exported="false"
            android:label="@string/title_activity_settings_rand"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".actis.Reportus"
            android:exported="false"
            android:theme="@style/Theme.AppCompat.Dialog" />
        <!--android:theme="@style/AppTheme"
        -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/paths" />
        </provider>

    </application>

</manifest>